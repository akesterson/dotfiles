;; ------------- Winner mode (so windows are less painful)

(load-file "~/.emacs.d/winnermode.el")

;; BLURGH, this doesn't work on RHEL ... will figure out why later
;; (load "/home/akesterson/.emacs.d/jinja.el")

(load-file "~/.emacs.d/emacs-options.el")

;; ------------- W3M hotness

(load-file "~/.emacs.d/w3m.el")

;; ------------ tags hotness

(load-file "~/.emacs.d/tags.el")

;; -------------- Window hotness

(load-file "~/.emacs.d/resize-window.el")


;; -------------- TRAMP hotness

(load-file "~/.emacs.d/tramp.el")

;; -------------- Indentation settings

(load-file "~/.emacs.d/whitespace.el")

;; -------------- startup crap

(load-file "~/.emacs.d/session.el")